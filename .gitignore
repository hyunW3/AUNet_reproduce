runs/

# large data / model artifacts (downloaded or generated, not source)
data/
tokenizer/

# model checkpoints (belt-and-suspenders: runs/ already ignored, but never
# let a stray checkpoint written elsewhere bloat the repo again)
checkpoints/
consolidated/
*.pth
*.pt
*.ckpt
*.safetensors
*.bin
*.distcp
*.metadata
tmp_global_ckpts/

# python virtualenvs
.venv/

# rendered animation media (regenerate from viz/*.py)
viz/*.gif
viz/*.mp4

# python bytecode caches
__pycache__/
*.pyc

# project page has its own git repo (BPEByte_project_page)
project_page/

# paper sources live in Overleaf; this tree is a working copy, not the origin
paper_overleaf/

# anonymised submission bundle — generated from the repo, not a source of truth
code_appendix/
code_appendix.zip
